import 'dart:ui';

class AppColors {
  // Background
  static const background = Color(0xFF0A0E21);

  // Cards
  static const card = Color(0xFF1D1E33);
  static const inactiveCard = Color(0xFF111328);

  // Accent (Pink)
  static const accent = Color(0xFFEB1555);

  // Text
  static const primaryText = Color(0xFFFFFFFF);
  static const secondaryText = Color(0xFF8D8E98);

  // Slider
  static const sliderActive = Color(0xFFEB1555);
  static const sliderInactive = Color(0xFF8D8E98);
  static const sliderThumb = Color(0xFFEB1555);

  // Buttons (plus/minus)
  static const button = Color(0xFF4C4F5E);
}